# file managed by puppet
<% if has_variable?("java_options") %>
# Parameters that should be set in setenv.sh
<% options = "" %>
<% scope.lookupvar('java_options').each do |hash| %>
<% hash.each do |key,value| %>export <%=key %>=<%=value%><% end %>
<% end %>
<% end %>
