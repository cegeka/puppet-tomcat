SERVER_PORT=$(expr 8050 + $TOMCAT_NUMBER)
HTTP_PORT=$(expr 8080 + $TOMCAT_NUMBER)
AJP_PORT=$(expr 8010 + $TOMCAT_NUMBER)
export TOMCAT_INSTANCE_PROPS="-Xmx256m -XX:MaxPermSize=128m -Dtn.tomcat.server.port=$SERVER_PORT -Dtn.tomcat.connector.http.port=$HTTP_PORT -Dtn.tomcat.connector.ajp.port=$AJP_PORT -Dtn.tomcat.connector.ajp.maxThreads=200 -Dtn.tomcat.engine.jvmRoute=${TOMCAT_NAME}${TOMCAT_SERVER_SUFFIX}"
